<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.meanwhile.ringmindme"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity">
        </activity>

        <provider
            android:name="com.meanwhile.ringmindme.provider.ActionProvider"
            android:authorities="com.meanwhile.ringmindme.provider"
            android:exported="false" />

        <receiver android:name="alarm.AlarmReceiver"></receiver>

        <receiver android:name=".alarm.BootReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
    </application>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

</manifest>